// Libraries
import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  ButtonGroup: {
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    paddingVertical: 10
  }
})
