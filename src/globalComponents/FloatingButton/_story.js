// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import FloatingButton from './index'

storiesOf('FloatingButton  - web', module)
  .add('default', () => (
    <FloatingButton />
  ))
