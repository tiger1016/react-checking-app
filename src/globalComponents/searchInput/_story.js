// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import SearchInput from './index'

storiesOf('SearchInput  - web', module)
  .add('default', () => (
    <SearchInput />
  ))
