// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import SaveCancel from './index'

storiesOf('SaveCancel  - web', module)
  .add('default', () => (
    <SaveCancel />
  ))
