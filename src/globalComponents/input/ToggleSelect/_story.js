// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import ToggleSelect from './index'

storiesOf('ToggleSelect  - web', module)
  .add('default', () => (
    <ToggleSelect />
  ))
