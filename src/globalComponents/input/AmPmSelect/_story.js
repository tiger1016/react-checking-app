// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import AmPmSelect from './index'

storiesOf('AmPmSelect  - web', module)
  .add('default', () => (
    <AmPmSelect />
  ))
