// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import GenderSelect from './index'

storiesOf('GenderSelect  - web', module)
  .add('default', () => (
    <GenderSelect />
  ))
