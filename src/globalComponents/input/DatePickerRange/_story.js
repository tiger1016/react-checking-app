// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import DatePickerRange from './index'

storiesOf('DatePickerRange  - web', module)
  .add('default', () => (
    <DatePickerRange />
  ))
