// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import DaysOfWeekSelect from './index'

storiesOf('DaysOfWeekSelect  - web', module)
  .add('default', () => (
    <DaysOfWeekSelect />
  ))
