// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import Checkbox from './index'

storiesOf('Checkbox  - web', module)
  .add('default', () => (
    <Checkbox />
  ))
