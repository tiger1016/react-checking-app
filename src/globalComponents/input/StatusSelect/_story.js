// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import StatusSelect from './index'

storiesOf('StatusSelect  - web', module)
  .add('default', () => (
    <StatusSelect />
  ))
