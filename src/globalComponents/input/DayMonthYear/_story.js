// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import DayMonthYear from './index'

storiesOf('DayMonthYear  - web', module)
  .add('default', () => (
    <DayMonthYear />
  ))
