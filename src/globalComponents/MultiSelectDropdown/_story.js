// Libraries
import React from 'react'
import { storiesOf } from '@storybook/react'
import MultiSelectDropdown from './index'

storiesOf('MultiSelectDropdown  - web', module)
  .add('default', () => (
    <MultiSelectDropdown />
  ))
